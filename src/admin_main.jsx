import React from "react";
import ReactDOM from "react-dom/client";
import AdminTokensApp from "./admin_tokens_app.jsx";
import "./admin_tokens.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AdminTokensApp />
  </React.StrictMode>,
);
